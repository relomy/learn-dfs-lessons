# A team-context lookup returns an explicit outcome

The learner built and tested both branches of a team-context lookup. They understand that a missing match must remain visible rather than becoming invented zero data, the lookup takes a domain team code instead of provider data, and checking the <code>kind</code> discriminant narrows the result to branch-specific properties for TypeScript.
