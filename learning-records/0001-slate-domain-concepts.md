# A salary row is not the domain model

The learner can distinguish a `Slate` as a set of games, a `Player` as an individual athlete, a `Matchup` as a game between two teams, and a `SlateEntry` as the relationship between a player and a particular slate. This establishes the first domain vocabulary and supports future work on source parsing, normalization, and position-specific evaluation.
